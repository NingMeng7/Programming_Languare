1.入口条件循环:for while: 在每轮循环计算测试表达式的值，决定是否执行循环
  出口条件循环:do...while:  首先执行循环体，再检测表达式的值
2.You will see:
0
1
2
3
4

3.You will see
0
3
6
9

4.You will see
6
8

5.You will see
k = 8

6.
for(int i = 1; i <= 64;i *= 2) {
  cout << i << endl;
}

7.花括号

8.
a)逗号表达式将会先计算左边一项，但是整体的值是右边那项，所以将024(十进制20)赋值给x
b)gg,逗号表达式的优先级最低，所以,024缺少标识符而出现语法错误

9.
cin>>ch:
a)  blank,tab,enter都会被视作结束标志
b)  badinput将会封锁输入

cin.get(ch):
a)  隐含了引用传送ch
b)  返回的是istream

ch = cin.get():
a)  ch被函数的返回值赋值
