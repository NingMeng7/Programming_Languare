1.  C++程序的模块叫做函数
2.  #include <iostream>的作用是：
    这将导致在最终的编译之前，使用iostream文件的内容替换该编译指令

3.  using namespace std; 的作用是：
    使得程序可以使用std名称空间中的定义
    
4.  cout << "Hello, world" << endl;
5.  int cheeses;
6.  cheeses = 32;
7.  cin >> cheeses;
8.  cout << "We have " << cheeses << " varieties of cheese" << endl;
9.  int froop(double t);  函数froop接受一个 double参数，该函数将返回一个 int 值
    void rattle(int n);   函数rattle接受一个int参数，不返回任何值  
    int prune(void);      函数prune不接受任何参数，返回一个 int 值
10. 类型为void的函数不需要return
    但是 return; 是合法的
11. using namespace std;
    std::cout;
    using std::cout;
