1.
a)  默认为自动变量
b)  在一个文件中声明secret为外部变量，在第二个文件通过extern声明来使用它
c)  在外部定义前加上关键字static, 将topsecret定义为一个内部链接的静态变量
d)  在函数声明的前面加上static, 将beencalled定义为一个链接性为内部的静态变量
2.  
a)  using 声明使得名称空间中的单个名称可用,其作用域与using所在的声明区域相同，其覆盖全局变量，并且当和局部名称冲突的时候会产生错误信息。
b)  using 编译指令使得整个名称空间内的所有名称可用，但是带来的问题是可能引入了一些不需要的名称，这些名称如果和局部名称冲突，将会被局部名称覆盖而不会产
    生任何错误信息。
3.  #include <iostream>
    int main()
    {
      double x;
      std::cout << "Please enter value: ";
      while(! (std::cin >> x) ) {
        std::cout << "Bad input. Please enter a number: ";
        std::cin.clear();
        while (std::cin.get()!='\n')
          continue;
      }
      std::cout << "Value = " << x << endl;
      return 0;
    }
4.  #include <iostream>
    int main()
    {
      double x;
      using std::cout;
      using std::cin;
      using std::endl;
      ....
    }
5.  
  in file1:
  int average(int a, int b)
  {
    return (a+b)/2;
  }
  in file2:
  static double average(int a, int b)
  {
    return (a+b)/2;
  }
